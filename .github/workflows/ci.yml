name: CI/CD Pipeline

on:
  push:
    branches: [ "master", "main" ]
  pull_request:
    branches: [ "master", "main" ]

jobs:
  backend-test:
    name: Backend Tests (Quarkus)
    runs-on: ubuntu-latest
    
    services:
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
          
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Java 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven
        
    - name: Run Backend Tests
      run: |
        chmod +x mvnw
        ./mvnw clean test
      working-directory: ./backend
      
    - name: Print Backend Test Logs
      if: failure()
      run: cat target/surefire-reports/*.txt || echo "No reports found"
      working-directory: ./backend

  frontend-test:
    name: Frontend Tests (Angular)
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Node.js 20
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: './frontend/package-lock.json'
        
    - name: Install Dependencies
      run: npm ci
      working-directory: ./frontend
      
    - name: Run Frontend Tests
      # Assuming Vitest or Karma, passing --watch=false ensures it runs once and exits
      run: npm run test -- --watch=false
      working-directory: ./frontend
